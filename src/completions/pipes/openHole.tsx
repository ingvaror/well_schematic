import * as React from "react"

interface Props {
    key: string
}

export class OpenHoleComponent extends React.Component<Props, {}> {
    render() {
        return (
            <>
            </>
        )
    }
}